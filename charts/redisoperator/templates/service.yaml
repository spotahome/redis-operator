apiVersion: v1
kind: Service
metadata:
  name: {{ include "name" . | quote }}
  labels:
    component: "{{ .Values.redisoperator.name }}"
{{ include "common-labels" . | indent 4 }}
spec:
  selector:
    component: "{{ .Values.redisoperator.name }}"
{{ include "common-labels" . | indent 4 }}
  ports:
  - name: metrics
    port: 9710
    protocol: TCP
